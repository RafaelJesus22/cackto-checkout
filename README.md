# Teste Front-End Cakto - Rafael Jesus

### Decisões técnicas

- Fazer o checkout como um micro módulo
  [] usar composition para montar o JSX
  [] estado global para o checkout
  [] criar uma interface que os métodos de pagamento devem implementar
  [] ações no servidor

- Detalhes técnicos
- [] - lib de UI (tailwind puro e alguns componentes do shadnc)
- [] - estado global do checkout (contextAPI acho que resolve)
- [] = actions para chamadas

### Transparência de Uso de IA

- IA implementou a função formatCurrency (utils/currency.ts)

### Regras de negócio

### Como executar

até agora npm install e npm run dev
acho que não vai mudar

### Para aumentar a conversão do checkout
